<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="basicTagView">
<!--    ' 등록된 기본태그 보여주기-->
<!--    ' 20170425 hwkim 새로 copy 작성-->
<!--    ' 20180531 hyeok 태그표시 버그 픽스(첫번째 태그 바로뒤에 ,가 붙지 않는 이슈)-->
    <select id="boardTag">
        SELECT b.btname
             , b.btidx
        FROM basictagmap a
          INNER JOIN basictag b ON a.btidx=b.btidx
         WHERE a.btype = #{tagType}
           AND a.bid = #{tagId}
           AND a.bidx = #{tagIdx}
        ORDER BY b.btname
    </select>
</mapper>