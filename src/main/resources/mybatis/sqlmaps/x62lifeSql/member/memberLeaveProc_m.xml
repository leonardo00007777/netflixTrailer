<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberLeaveProc">
<!--    '프로젝트: 자연이랑 모바일 웹-->
<!--    '프로그램: 회원탈퇴 처리-->

<!--    '20170314   sykim   처음작성-->
    <select id="getMemberPassword">
        UPDATE mb_master
        SET memstcd = #{memstOutreq}
          , retiremsg = #{strRetireMSG}
          , updt = GETDATE()
          , updid = #{strLoginMEMCD}
         WHERE memcd = #{strLoginMEMCD}
    </select>
</mapper>