<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="searchPhoneAuthNoSend">
<!--    '프로젝트: 자연이랑 모바일 웹-->
<!--    '프로그램: 비밀번호 찾기 - 휴대전화-->

<!--    '20171013   sykim   처음작성-->
    
    <select id="getPhoneNumber" statementType="CALLABLE">
        /*getPhoneNumber 휴대폰번호 가져오기*/
       {CALL encryptionOpen
        SELECT CONVERT(VARCHAR(14)
             , DECRYPTBYKEY(hpno_enc)) hpno
        FROM mb_master
         WHERE memcd = #{strMemCd}
            CALL encryptionClose}
    </select>
</mapper>