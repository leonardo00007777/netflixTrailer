<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="searchPwdPhoneConf">
<!--    '프로젝트: 자연이랑 모바일 웹-->
<!--    '프로그램: 비밀번호 찾기 - 휴대전화 인증번호 확인.-->

<!--    '20170309   sykim   처음작성-->
    <select id="getAuthNum">
        SELECT authno
        FROM smsauthno
         WHERE memcd = #{strMemCd}
           <![CDATA[
           AND DATEDIFF(SS,senddate,GETDATE()) <= #{LIMITSECOND}
           ]]>
    </select>

    <delete id="authConfirm">
        /* authConfirm 인증성공 시 인증정보 삭제*/
        DELETE FROM smsauthno WHERE memcd = #{strMemCd}
    </delete>
</mapper>