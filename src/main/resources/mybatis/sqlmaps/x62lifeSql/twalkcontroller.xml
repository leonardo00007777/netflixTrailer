<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="getuserid">
    <select id="checkAllAccessTokenInfo">
     SELECT token
     FROM TOKEN_TABLE
      WHERE userid='*'
        AND t_type='A'
        <![CDATA[
        AND expires >= getdate()
        ]]>
        AND t_name = 'TWALKAPP'
        AND scope_read='Y'
    </select>
</mapper>